# Application Settings
APP_NAME=SMS-POWERBOMB
APP_VERSION=10.0.0
DEBUG=True
ENVIRONMENT=development

# Server Settings
HOST=0.0.0.0
PORT=8000

# Database Settings
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/smsbomb
DATABASE_ECHO=False

# Redis Settings
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# JWT Settings
SECRET_KEY=your-secret-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS Settings
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
CORS_CREDENTIALS=True
CORS_METHODS=*
CORS_HEADERS=*

# Rate Limiting
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_PER_HOUR=1000

# SMS API Settings
MAX_CONCURRENT_REQUESTS=50
REQUEST_TIMEOUT=30
RETRY_ATTEMPTS=3
RETRY_DELAY=2

# Campaign Settings
MAX_TARGETS_PER_CAMPAIGN=1000
MAX_WAVES=100
MIN_DELAY_SECONDS=1
MAX_DELAY_SECONDS=60